A minishell based on tcsh

TODO :

Must :
- good return
- "|", "<", , "<<", ">", ">>" support;
- parsing with binary tree

Should :
- "~" for $HOME
- arrows key support (use history);
- ctrl + C -> new prompt;
- echo $?
- allocs == free
